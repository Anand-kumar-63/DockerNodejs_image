// fisrt clone the repo
// Server is running locally and mongodb conatiner is running on port 27017
// You have to run mongodb docker images - docker run -p 27017:27017 mongo 
// Then connect the mongo container to mongodb compass inside your cluster using the url - mongodb://mongo:27017/<Database_name>
// Now build the nodejs server using - npm run build
// Now run the nodejs Server locally using - npm start
// now test by creating the user using postman  